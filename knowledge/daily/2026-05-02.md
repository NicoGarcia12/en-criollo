# Daily — 2026-05-02

## Hecho

- Se relevó el pedido de consolidar URLs de producción (había 2, objetivo: dejar 1 efectiva).
- Se investigó dónde quedó definido testing/quality en la documentación actual.
- Se implementó GREEN para 9 tests RED de UI/API:
  - UI solo en español (se removió EN en dictionary/locales y toggle en home),
  - copy de valor explícito para entender mensajes + ayuda opcional para responder,
  - footer con marca "En Criollo" en `var(--neon)` y crédito profesional de autoría,
  - prompting multilenguaje condicional (solo español vs español + idioma original) con reglas de claridad para usuario final.
- Se estandarizaron scripts de ejecución en Next.js para evitar confusión dev/prod:
  - `npm start` ahora corre `next dev` (watch/hot reload),
  - se agregó `npm run start:prod` para `next start` (producción),
  - README actualizado con uso recomendado.
- Ajuste UI responsive en home:
  - se amplió moderadamente el ancho general (`max-w-3xl` → `max-w-4xl`) en header/hero/app/footer,
  - footer con comportamiento adaptable `flex-wrap`: fila cuando entra y fallback natural cuando no entra,
  - se conservó estilo de marca (`En Criollo` neón solo en bloque izquierdo).
- Corrección de copy en propuesta de valor (hero/subtitle):
  - se eliminó la formulación "opcionalmente" sobre responder,
  - se dejó explícito que En Criollo ayuda en dos funcionalidades distintas: entender mensajes y redactar respuestas.

## Hallazgos

- Producción corre en Vercel.
- Estrategia segura para dejar 1 URL efectiva:
  - auditar `domains`/`aliases` activos,
  - definir una URL canónica,
  - remover sobrante con `alias rm` o `domain rm` (según corresponda),
  - aplicar redirect si hay tráfico/links que preservar.
- En knowledge, testing/quality aparece distribuido en `learnings/`, `playbooks/`, `sessions/` y `daily/`, sin una source of truth única.

## Pendientes

- Ejecutar la consolidación operativa de URLs en Vercel con validación post-cambio.

## Siguiente paso sugerido

- Crear un playbook canónico de quality gate (por ejemplo: `knowledge/playbooks/quality-gate.md`) para centralizar criterios y flujo de testing/quality.
